import {useLayoutEffect, useState} from 'react'

export default function useTimeBlockSize({start, end}){
    //this gets the height of the time block and 
    //returns the appropriate amount for styling
}

export default function useScreenSizeForLayout(){
    const [size, setSize] = useState([0, 0])
    //window.innerWidth
    //switch case and return hook that controls layout??
    //300 - 450px - 3 COL
    //450px - 750px - 5 COL
    //750px - ALL COL, no header
}

export default function useCalendarLayout(){
    //check screen size, determine if hiding and showing buttons
    //if not, hide buttons 
}